# Best-50 Table Plan

## Goal
Add a static, server-rendered page that shows Asia's 50 Best Bars in a simple HTML table.

## Checklist
- [ ] Create the data module at `features/best-50/best-50-data.ts` with 50 entries.
- [ ] Build the presentational table at `features/best-50/best-50-table.tsx`.
- [ ] Build the container at `features/best-50/best-50-container.tsx`.
- [ ] Add the route at `app/best-50/page.tsx`.
- [ ] Manually verify `/best-50` renders 50 rows.
